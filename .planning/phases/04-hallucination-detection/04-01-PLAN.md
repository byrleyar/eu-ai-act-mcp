---
phase: 04-hallucination-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - citation_schema.py
  - pdf_generator.py
  - tests/test_citation_schema.py
  - tests/test_pdf_generator.py
autonomous: true

must_haves:
  truths:
    - "HALLUCINATED is accepted as a valid confidence level when creating a Citation"
    - "PDF report renders HALLUCINATED citations with bold red visual treatment distinct from NOT FOUND pink"
    - "Executive summary confidence breakdown includes HALLUCINATED count and percentage"
    - "All 57 existing tests continue to pass with no changes to their assertions"
  artifacts:
    - path: "citation_schema.py"
      provides: "HALLUCINATED enum value in ConfidenceLevel"
      contains: "HALLUCINATED"
    - path: "pdf_generator.py"
      provides: "HALLUCINATED color, formatting branch, and executive summary entry"
      contains: "HALLUCINATED"
    - path: "tests/test_citation_schema.py"
      provides: "Tests validating HALLUCINATED as accepted confidence level"
      contains: "HALLUCINATED"
    - path: "tests/test_pdf_generator.py"
      provides: "Tests for HALLUCINATED PDF rendering and executive summary inclusion"
      contains: "HALLUCINATED"
  key_links:
    - from: "citation_schema.py"
      to: "pdf_generator.py"
      via: "HALLUCINATED string value used in CONFIDENCE_COLORS dict and formatting branches"
      pattern: "HALLUCINATED"
    - from: "pdf_generator.py"
      to: "CONFIDENCE_COLORS"
      via: "HALLUCINATED key in color dict drives row background color"
      pattern: "'HALLUCINATED'.*HexColor"
    - from: "pdf_generator.py"
      to: "_build_executive_summary"
      via: "HALLUCINATED included in confidence breakdown loop"
      pattern: "HALLUCINATED.*in.*for level"
---

<objective>
Add HALLUCINATED as the 5th confidence level throughout the citation system: schema enum, PDF color/formatting, executive summary breakdown, and comprehensive tests.

Purpose: Users need to see which compliance answers are fabricated (not supported by any model card source), with unmistakable bold red visual treatment in the PDF report so hallucinated answers cannot be confused with legitimate citations.

Output: Updated citation_schema.py, pdf_generator.py, and their test files with full HALLUCINATED support.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@citation_schema.py
@pdf_generator.py
@tests/test_citation_schema.py
@tests/test_pdf_generator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HALLUCINATED to citation schema and schema tests</name>
  <files>citation_schema.py, tests/test_citation_schema.py</files>
  <action>
In citation_schema.py:
- Add `HALLUCINATED = "HALLUCINATED"` to the ConfidenceLevel enum, after NOT_FOUND. This is the 5th enum value alongside DIRECT, INFERRED, DEFAULT, NOT_FOUND.

In tests/test_citation_schema.py:
- Update `test_valid_citation_all_confidence_levels` docstring from "All 4 ConfidenceLevel values" to "All 5 ConfidenceLevel values" (the test body already iterates `ConfidenceLevel` so it will automatically cover the new value).
- Add a new test `test_valid_citation_hallucinated_confidence`:
  - Creates a Citation with `confidence=ConfidenceLevel.HALLUCINATED`, question_id="Q1", question_text="Test?", reasoning="No supporting source found in model card"
  - Asserts `citation.confidence == ConfidenceLevel.HALLUCINATED`
  - Asserts `citation.confidence.value == "HALLUCINATED"`
- Add a new test `test_validate_json_hallucinated_confidence`:
  - Creates a JSON string with a citation using `"confidence": "HALLUCINATED"`
  - Calls `validate_citation_json()` and asserts it returns a CitationReport
  - Asserts the first citation's confidence equals `ConfidenceLevel.HALLUCINATED`

Do NOT modify any existing test assertions or logic. The existing test for "MAYBE" rejection (`test_reject_invalid_confidence`) must continue to work unchanged.
  </action>
  <verify>
Run `python -m pytest tests/test_citation_schema.py -v` and confirm all tests pass including the new HALLUCINATED tests. Verify test count increased from the original count.
  </verify>
  <done>
ConfidenceLevel enum has 5 values (DIRECT, INFERRED, DEFAULT, NOT_FOUND, HALLUCINATED). Citation objects accept HALLUCINATED as a valid confidence level via both Python enum and JSON string input. All existing schema tests pass unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add HALLUCINATED rendering to PDF generator and PDF tests</name>
  <files>pdf_generator.py, tests/test_pdf_generator.py</files>
  <action>
In pdf_generator.py, make three changes:

1. **CONFIDENCE_COLORS dict** (line ~33): Add `'HALLUCINATED': colors.HexColor('#FF0000')` (pure red). This is intentionally more intense than NOT FOUND's pink (#F8D7DA) to create unmistakable visual distinction. The dict will now have 5 entries.

2. **Citation formatting branch** (line ~254-270): Add an `elif confidence == 'HALLUCINATED':` branch BEFORE the else fallback. Format:
   ```python
   elif confidence == 'HALLUCINATED':
       source_details = f"<b>WARNING: No supporting source found.</b><br/><br/>Reasoning: {_escape_xml(reasoning)}"
   ```
   Also update the section_display logic (line ~273) to include HALLUCINATED alongside DEFAULT and NOT FOUND for showing '-' in the section column:
   ```python
   if confidence in ['DEFAULT', 'NOT FOUND', 'HALLUCINATED']:
       section_display = '-'
   ```

3. **Executive summary breakdown** (line ~137): Update the `for level in [...]` loop to include HALLUCINATED:
   ```python
   for level in ['DIRECT', 'INFERRED', 'DEFAULT', 'NOT FOUND', 'HALLUCINATED']:
   ```

In tests/test_pdf_generator.py, add these tests:

1. **test_pdf_hallucinated_citation_rendering**: Create a citation with confidence='HALLUCINATED', answer='Some fabricated answer', reasoning='No supporting evidence found in model card documentation'. Generate PDF, extract text, assert 'WARNING' or 'No supporting source' appears in normalized text, and assert the reasoning text appears.

2. **test_pdf_hallucinated_in_executive_summary**: Create citations including at least one HALLUCINATED citation. Generate PDF, extract text from page 1, assert 'HALLUCINATED' appears in the executive summary text (confirming it shows in the confidence breakdown).

3. **test_pdf_confidence_colors_includes_hallucinated**: Update or add a test asserting `len(CONFIDENCE_COLORS) == 5` and `'HALLUCINATED' in CONFIDENCE_COLORS`. NOTE: The existing test `test_pdf_confidence_colors_imported` asserts `len(CONFIDENCE_COLORS) == 4` -- this MUST be updated to `== 5` and add `assert 'HALLUCINATED' in CONFIDENCE_COLORS`.

4. **test_pdf_all_confidence_levels**: The existing test creates citations for 4 levels. Update the `confidence_levels` list to include 'HALLUCINATED' as the 5th entry so it verifies all 5 levels render in the PDF.

5. **test_pdf_hallucinated_section_shows_dash**: Create a HALLUCINATED citation with a non-empty source_section. Generate PDF, extract text. Verify the section column shows '-' (not the actual section value), confirming HALLUCINATED is treated like DEFAULT/NOT FOUND for section display.

For the `many_citations` fixture: Update the confidence level cycle from `['DIRECT', 'INFERRED', 'DEFAULT', 'NOT FOUND'][i % 4]` to `['DIRECT', 'INFERRED', 'DEFAULT', 'NOT FOUND', 'HALLUCINATED'][i % 5]` so multi-page tests exercise the new level.

Do NOT change any other existing test logic beyond the specific updates noted above (confidence_colors count, all_confidence_levels list, many_citations fixture).
  </action>
  <verify>
Run `python -m pytest tests/ -v` and confirm ALL tests pass (both test_citation_schema.py and test_pdf_generator.py). The total test count should be higher than the original 57. Verify no existing test names were removed.
  </verify>
  <done>
CONFIDENCE_COLORS has 5 entries including HALLUCINATED with pure red (#FF0000). HALLUCINATED citations render with bold "WARNING: No supporting source found" text. Executive summary shows HALLUCINATED count in breakdown. Section column shows '-' for HALLUCINATED. All tests pass including both new HALLUCINATED-specific tests and all pre-existing tests.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run full test suite: `python -m pytest tests/ -v`
   - All tests pass (0 failures)
   - Test count > 57 (original baseline)
   - No test names from the original 57 are missing

2. Verify schema: `python -c "from citation_schema import ConfidenceLevel; print([e.value for e in ConfidenceLevel])"`
   - Output includes: DIRECT, INFERRED, DEFAULT, NOT FOUND, HALLUCINATED

3. Verify PDF colors: `python -c "from pdf_generator import CONFIDENCE_COLORS; print(list(CONFIDENCE_COLORS.keys()))"`
   - Output includes all 5 levels including HALLUCINATED

4. Verify executive summary inclusion: `python -c "from pdf_generator import _build_executive_summary; print('OK')"` (function importable, no syntax errors)
</verification>

<success_criteria>
- HALLUCINATED is the 5th valid ConfidenceLevel enum value (HALL-01)
- PDF renders HALLUCINATED with red background (#FF0000) and bold warning text (HALL-02)
- Executive summary includes HALLUCINATED in confidence breakdown statistics (HALL-04)
- All original 57 tests pass without modification to their core assertions (regression-free)
- New tests specifically validate HALLUCINATED behavior in both schema and PDF
</success_criteria>

<output>
After completion, create `.planning/phases/04-hallucination-detection/04-01-SUMMARY.md`
</output>
