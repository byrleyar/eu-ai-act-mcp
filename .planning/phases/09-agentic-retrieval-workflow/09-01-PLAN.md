# Phase 09 Plan 01: Agentic Retrieval Workflow Overhaul

**Goal**: Update `context.md` with strict instructions for the "Discovery -> Gap Analysis -> Targeted Fetch" retrieval loop to improve compliance report fidelity.

## Proposed Changes

### 1. `context.md`
- **Rewrite the Workflow Section**:
    - Step 1: Initial Discovery (`fetch_hf_model_card`).
    - Step 2: Gap Analysis (Compare card text to `get_compliance_requirements`).
    - Step 3: Targeted Fetch (`fetch_external_document` for promising links).
    - Step 4: Final Generation (`generate_compliance_doc` -> `generate_source_report`).
- **Add Retrieval Principles**:
    - Proactive investigation of technical reports.
    - Context budgeting (avoid fetching everything).
    - Relevance judging using link context.
- **Preserve Existing Rules**:
    - Categorized question checklist.
    - Self-verification protocol (`HALLUCINATED` flag).
    - Full coverage requirement (80+ IDs).
    - Just-in-time enforcement rules.

## Implementation Steps

### Task 1: Draft the new `context.md`
1. Incorporate the new `fetch_external_document` tool into the sequence.
2. Add explicit "Gap Analysis" instructions.
3. Refine the retrieval logic to be punchy and clear.

### Task 2: Update the file
1. Replace the contents of `context.md` with the agentic version.

### Task 3: Verification
1. Verify the ID checklist matches `questions.json`.
2. Ensure the instructions for `fetch_external_document` are clear.

## Success Criteria
- [ ] `context.md` instructs Claude to fetch missing docs before answering.
- [ ] The workflow sequence is updated to include the fetch tool.
- [ ] All 80+ question IDs are still present and categorized.
- [ ] Instructions for context efficiency (budgeting) are included.
