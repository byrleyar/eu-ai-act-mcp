# Phase 06 Plan 01: Overhaul context.md for v1.2 Reliability

**Goal**: Update `context.md` with strict instructions for self-verification, full coverage (80+ questions), and reliable two-step tool execution.

## Proposed Changes

### 1. `context.md`
- **Add Self-Verification Protocol**: Explicit instructions to flag fabricated answers as `HALLUCINATED`.
- **Add Full Coverage Rule**: Instructions to include every ID from `questions.json` in the source report, using `NOT FOUND` for missing data.
- **Update Confidence Levels**: Include `HALLUCINATED` definition and usage criteria.
- **Refine Tool Sequencing**: Stronger language for the `generate_compliance_doc` -> `generate_source_report` sequence.
- **Add Link Presentation Rule**: Present both download links together in the final response.
- **Include Question ID Checklist**: A complete list of all question IDs for Claude to use as a reference.

### 2. `server.py`
- **Just-in-Time Enforcement**: Update `generate_compliance_doc` to return a strict system instruction commanding Claude to immediately call `generate_source_report`.
- **Schema Reminder**: Include a brief citation JSON schema reminder in the tool output.

## Implementation Steps

### Task 1: Draft the new `context.md`
1. Load `questions.json` to get the current list of IDs.
2. Integrate the `HALLUCINATED` level added in Phase 4.
3. Integrate the coverage requirements from Phase 5.
4. Rewrite the workflow section for maximum reliability.

### Task 2: Update `server.py`
1. Modify `generate_compliance_doc` return message to enforce the 2-step sequence.

### Task 3: Update `context.md` file
1. Replace the contents of `context.md` with the new version.

### Task 4: Manual Verification
1. Roleplay as the Compliance Officer with the new instructions to ensure they are clear and executable.

## Success Criteria
- [ ] `context.md` contains the `HALLUCINATED` confidence level.
- [ ] `context.md` instructs Claude to include all 80+ question IDs in the source report.
- [ ] `context.md` defines a strict 2-step tool sequence.
- [ ] `context.md` instructs Claude to present both links together.
- [ ] `server.py` provides just-in-time instructions for the second tool call.
