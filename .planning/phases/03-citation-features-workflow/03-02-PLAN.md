---
phase: 03-citation-features-workflow
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [context.md, tests/test_pdf_generator.py]
autonomous: true

must_haves:
  truths:
    - "context.md instructs Claude to track source citations during compliance form generation"
    - "context.md instructs Claude to call generate_source_report after generate_compliance_doc"
    - "context.md includes JSON example with all citation fields and confidence level definitions"
    - "Tests verify executive summary contains confidence breakdown counts"
    - "Tests verify confidence-based row colors are applied"
    - "Tests verify footer contains model card identifier"
    - "Tests verify citation content varies by confidence type"
  artifacts:
    - path: "context.md"
      provides: "Workflow instructions for Claude to track and report citations"
      contains: "generate_source_report"
    - path: "tests/test_pdf_generator.py"
      provides: "Test coverage for Phase 3 citation features"
      contains: "CONFIDENCE_COLORS"
  key_links:
    - from: "context.md"
      to: "server.py"
      via: "Tool call instructions referencing generate_source_report and model_card_id"
      pattern: "generate_source_report"
    - from: "tests/test_pdf_generator.py"
      to: "pdf_generator.py"
      via: "Import and test of CONFIDENCE_COLORS, executive summary, footer"
      pattern: "CONFIDENCE_COLORS"
---

<objective>
Update workflow instructions in context.md for citation tracking and add comprehensive tests verifying all Phase 3 visual formatting features.

Purpose: Complete the Phase 3 deliverables by (1) enabling the end-to-end workflow where Claude tracks citations and generates reports, and (2) proving all CITE requirements are satisfied through automated tests.

Output: Updated `context.md` with citation tracking instructions; extended `tests/test_pdf_generator.py` with Phase 3 feature tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-citation-features-workflow/03-RESEARCH.md
@.planning/phases/03-citation-features-workflow/03-01-SUMMARY.md
@context.md
@tests/test_pdf_generator.py
@pdf_generator.py
@server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update context.md with citation tracking workflow instructions</name>
  <files>context.md</files>
  <action>
Append a new section to the END of context.md (after the existing content about recommended lengths). Do NOT modify any existing content in context.md.

Add the following section:

**"## Citation Tracking"** section with these subsections:

**1. "### Track Your Sources"** -- Instruct Claude that while filling out the compliance form, it MUST track where each answer came from. For each question answered, record:
- question_id (from questions.json, e.g., "model_architecture")
- question_text (the full question)
- answer (what was put in the compliance form)
- source_quote (exact text from model card, if available; empty string if not)
- source_section (heading where found, e.g., "Model Architecture"; empty string if not applicable)
- confidence level with clear definitions:
  - **DIRECT**: Answer is a verbatim quote or close paraphrase of an explicit statement in the model card
  - **INFERRED**: Answer derived from related information in the model card, with reasoning explaining the derivation
  - **DEFAULT**: Standard or assumed value appropriate for the context (e.g., version 1.0 for initial release)
  - **NOT FOUND**: Information was searched for in the model card but not located
- reasoning (why this answer and confidence level were chosen)

**2. "### When to Generate Source Report"** -- Instruct Claude:
- **IMMEDIATELY after calling `generate_compliance_doc`**, call `generate_source_report` with:
  - `source_citations_json`: JSON string with all tracked citations
  - `model_name`: Same model name used in the compliance form
  - `model_card_id`: The Hugging Face model ID (e.g., "meta-llama/Llama-3.1-405B")
- Include a complete JSON example showing a DIRECT citation and a NOT FOUND citation (with all 7 fields)
- State that the citation report is a **core deliverable** and must not be skipped

**3. "### Why This Matters"** -- Brief explanation: The source citation report creates an audit trail showing exactly where each compliance answer came from, enabling auditors to verify answers against the model card.

Use the JSON example from the research file (03-RESEARCH.md Pattern 5) as the template, but ensure it matches the actual Pydantic schema fields from citation_schema.py.

Keep the tone directive and assertive (matching existing context.md style: "You MUST", "Do NOT skip").
  </action>
  <verify>
Verify context.md contains the new sections:
```bash
grep -c "Citation Tracking" context.md
grep -c "generate_source_report" context.md
grep -c "model_card_id" context.md
grep -c "DIRECT" context.md
grep -c "NOT FOUND" context.md
```
All counts should be >= 1.

Verify existing content is preserved:
```bash
grep -c "Do not interview the user" context.md
grep -c "Compliance Officer" context.md
```
Both should be >= 1 (unchanged).
  </verify>
  <done>
context.md contains citation tracking instructions with confidence level definitions, JSON example, tool call sequence (generate_compliance_doc then generate_source_report), and model_card_id parameter guidance. Existing content unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive tests for Phase 3 citation features</name>
  <files>tests/test_pdf_generator.py</files>
  <action>
Add new test functions to `tests/test_pdf_generator.py` covering all Phase 3 CITE requirements. Add necessary imports at the top: `from collections import Counter`.

**Tests to add:**

1. **test_pdf_executive_summary_present** -- Generate PDF with 4 citations (one per confidence level). Extract text from page 1. Assert "Executive Summary" appears. Assert "DIRECT" count text appears. Assert model_card_id text appears.

2. **test_pdf_executive_summary_confidence_breakdown** -- Generate PDF with known citation distribution (e.g., 2 DIRECT, 1 INFERRED, 1 NOT FOUND). Extract text. Verify the count for each level appears in the text (e.g., "2" near "DIRECT").

3. **test_pdf_confidence_colors_imported** -- Import CONFIDENCE_COLORS from pdf_generator. Assert it has exactly 4 keys: 'DIRECT', 'INFERRED', 'DEFAULT', 'NOT FOUND'. Assert each value is a Color object.

4. **test_pdf_question_id_in_first_column** -- Generate PDF with a citation having question_id='model_architecture'. Extract text. Assert 'model_architecture' appears in the output (CITE-08 cross-referencing).

5. **test_pdf_direct_citation_has_quote** -- Generate PDF with DIRECT citation. Extract text. Verify the source_quote text appears in the output.

6. **test_pdf_not_found_citation_format** -- Generate PDF with NOT FOUND citation (empty answer, empty source_quote, empty source_section). Verify "not found" or "Information not found" appears in extracted text. Verify reasoning text appears.

7. **test_pdf_footer_has_model_card_id** -- Generate PDF with model_card_id='test-org/test-model'. Extract text from page 1. Assert 'test-org/test-model' appears OR 'Model Card' appears (footer text extraction can be imprecise).

8. **test_pdf_footer_on_multiple_pages** -- Generate PDF with 30 citations and model_card_id. Extract text from last page. Assert footer content (Model Card or page number) appears on last page.

9. **test_pdf_generate_with_model_card_id_parameter** -- Call `generate_source_report_pdf(buf, citations, model_card_id='org/model')`. Verify no exception raised and PDF is valid.

10. **test_pdf_default_citation_reasoning_explains_why_standard** (CITE-04 semantic verification) -- Generate PDF with a DEFAULT citation whose reasoning is `"Standard version number for initial release — no version history found in model card"`. Extract text. Assert the reasoning text appears in the output AND that the phrase "Standard value" or "standard" appears (confirming the formatting slot contains content explaining WHY the standard value is appropriate, not just a blank slot).

11. **test_pdf_inferred_citation_reasoning_shows_derivation** (CITE-03 semantic verification) -- Generate PDF with an INFERRED citation whose source_quote is `"Model was trained on 2 trillion tokens"` and reasoning is `"Training data size suggests large-scale web corpus based on typical data-to-token ratios"`. Extract text. Assert the reasoning text appears AND that the source_quote text appears (confirming the formatting renders both the related quote AND the derivation reasoning, not just one or neither).

**Important test patterns** (follow existing conventions in the file):
- Use `BytesIO()` for output stream
- Use `PdfReader` to verify PDF content
- Use existing `sample_citation` and `many_citations` fixtures where applicable
- Normalize extracted text with `' '.join(text.split())` for whitespace-insensitive matching
- Keep assertions focused on one requirement per test

Do NOT modify any existing test functions. Only ADD new ones.
  </action>
  <verify>
Run the full test suite:
```bash
cd /Users/andrew/Antigravity/ai_act_compliance_mcp_server && python -m pytest tests/test_pdf_generator.py -v
```
All tests must pass (12 existing + 11 new = 23 total).

Run full project test suite:
```bash
cd /Users/andrew/Antigravity/ai_act_compliance_mcp_server && python -m pytest tests/ -v
```
All tests across all test files must pass.
  </verify>
  <done>
11 new tests verify all Phase 3 CITE requirements: executive summary presence and confidence breakdown, confidence color constants, question_id cross-referencing, citation format per confidence type (including CITE-03 INFERRED derivation reasoning and CITE-04 DEFAULT standard-value reasoning with semantic content assertions), footer with model_card_id on single and multi-page reports, model_card_id parameter acceptance. All tests pass alongside 12 existing tests.
  </done>
</task>

</tasks>

<verification>
1. context.md contains citation tracking section with confidence level definitions
2. context.md instructs Claude to call generate_source_report after generate_compliance_doc
3. context.md includes complete JSON example matching citation_schema.py fields
4. All new tests pass verifying CITE-01 through CITE-09 requirements
5. All existing tests pass (no regressions)
6. FLOW-01 satisfied: context.md instructs Claude to track citations
7. FLOW-02 satisfied: context.md describes generate_source_report as independently callable
</verification>

<success_criteria>
- FLOW-01: context.md contains citation tracking instructions with confidence definitions and JSON example
- FLOW-02: context.md describes calling generate_source_report after generate_compliance_doc with model_card_id
- All 11 CITE/FLOW requirements have at least one test covering them
- All tests pass (existing + new) with zero regressions
- context.md existing content is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-citation-features-workflow/03-02-SUMMARY.md`
</output>
