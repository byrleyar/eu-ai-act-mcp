---
phase: 03-citation-features-workflow
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [pdf_generator.py, citation_schema.py, context.md, tests/test_citation_schema.py, tests/test_pdf_generator.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "DIRECT citations display source quotes in italics"
    - "Citations include source_document field identifying which document contained the quote"
    - "PDF generator displays source_document in citation rows"
  artifacts:
    - path: "fonts/DejaVuSans-Oblique.ttf"
      provides: "Italic font variant for ReportLab"
      min_lines: 0
    - path: "pdf_generator.py"
      provides: "Font family registration with italic mapping"
      contains: "registerFontFamily"
    - path: "citation_schema.py"
      provides: "source_document field in Citation model"
      contains: "source_document"
    - path: "context.md"
      provides: "source_document in JSON example"
      contains: "source_document"
  key_links:
    - from: "pdf_generator.py"
      to: "fonts/DejaVuSans-Oblique.ttf"
      via: "font registration"
      pattern: "registerFont.*DejaVuSans-Oblique"
    - from: "pdf_generator.py"
      to: "citation_schema.py"
      via: "source_document field usage"
      pattern: "source_document"
---

<objective>
Add italic font support and source_document tracking to citation schema and PDF generation.

Purpose: Enable proper visual formatting (italics for DIRECT quotes) and complete source attribution (tracking which document — model card, PDF attachment, or user context — contained each quote).

Output: Citations that display italicized quotes and identify both source document and section location.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-citation-features-workflow/03-01-SUMMARY.md
@.planning/phases/03-citation-features-workflow/03-02-SUMMARY.md
@.planning/phases/03-citation-features-workflow/03-UAT.md

@pdf_generator.py
@citation_schema.py
@context.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add italic font support to PDF generator</name>
  <files>
    fonts/DejaVuSans-Oblique.ttf
    pdf_generator.py
  </files>
  <action>
    Download DejaVuSans-Oblique.ttf italic font variant to fonts/ directory.

    Source: https://github.com/dejavu-fonts/dejavu-fonts/raw/master/ttf/DejaVuSans-Oblique.ttf

    In pdf_generator.py after line 28 (current registerFont call):
    - Add: `pdfmetrics.registerFont(TTFont('DejaVuSans-Oblique', os.path.join(FONT_DIR, 'DejaVuSans-Oblique.ttf')))`
    - Add: `pdfmetrics.registerFontFamily('DejaVuSans', normal='DejaVuSans', italic='DejaVuSans-Oblique')`

    This enables ReportLab to use the italic variant when `<i>` tags are encountered in Paragraph markup instead of falling back to regular weight.

    Location: Immediately after line 28 where DejaVuSans.ttf is registered.
  </action>
  <verify>
    1. `ls fonts/DejaVuSans-Oblique.ttf` confirms file exists
    2. `grep -n "registerFontFamily" pdf_generator.py` shows font family registration
    3. `python -c "from reportlab.pdfbase import pdfmetrics; import pdf_generator; print(pdfmetrics.getFont('DejaVuSans-Oblique').fontName)"` prints "DejaVuSans-Oblique"
  </verify>
  <done>
    DejaVuSans-Oblique.ttf exists in fonts/, pdf_generator.py registers italic font variant with family mapping, ReportLab can render italic text in PDFs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add source_document field to citation schema and update PDF/context</name>
  <files>
    citation_schema.py
    pdf_generator.py
    context.md
    tests/test_citation_schema.py
    tests/test_pdf_generator.py
  </files>
  <action>
    **citation_schema.py:**
    - Add new field to Citation model after source_section (around line 34): `source_document: str = Field(default="")`
    - This field tracks which document (model card, PDF attachment, user context) contained the source quote

    **pdf_generator.py:**
    - In _format_citation_content function (around lines 150-200), update DIRECT citation formatting to include source_document
    - Current format: `Source: <i>{quote}</i><br/>Section: {section}`
    - New format: `Source: <i>{quote}</i><br/>Document: {source_document}<br/>Section: {section}`
    - Use citation.get('source_document', '') with fallback for backward compatibility
    - Only show "Document:" line if source_document is non-empty

    **context.md:**
    - Update Citation Tracking section (around line 20-29) to include source_document field in the list
    - Update JSON example (around line 48-60) to include `"source_document": "Model Card"` in the DIRECT citation example and `"source_document": ""` in the NOT FOUND example

    **tests/test_citation_schema.py:**
    - Add test case verifying source_document field accepts strings and defaults to empty string
    - Test: `test_citation_with_source_document` validating full citation with all fields including source_document

    **tests/test_pdf_generator.py:**
    - Update test_pdf_with_direct_citation (if exists) to include source_document in citation data
    - Add assertion verifying "Document:" appears in PDF text when source_document is non-empty
  </action>
  <verify>
    1. `python -c "from citation_schema import Citation; c = Citation(question_id='test', question_text='Q', confidence='DIRECT', reasoning='R', source_document='Model Card'); print(c.source_document)"` prints "Model Card"
    2. `grep -n "source_document" citation_schema.py context.md pdf_generator.py` shows field in all three files
    3. `python -m pytest tests/test_citation_schema.py -v -k source_document` passes
    4. `python -m pytest tests/test_pdf_generator.py -v` passes all tests
  </verify>
  <done>
    Citation schema includes source_document field, PDF generator displays it in DIRECT citations below the quote, context.md instructs Claude to track it, tests verify the field works correctly.
  </done>
</task>

</tasks>

<verification>
1. Run full test suite: `python -m pytest tests/ -v`
2. Generate a test PDF with DIRECT citation including source_document and verify:
   - Quote text appears in italics (visually distinct from regular text)
   - "Document: Model Card" line appears below the quote
   - "Section: Architecture" line appears below document
3. Check that existing citations without source_document still render correctly (backward compatibility)
</verification>

<success_criteria>
- fonts/DejaVuSans-Oblique.ttf exists and is registered with ReportLab
- pdf_generator.py uses registerFontFamily to map italic variant
- Citation model has source_document field with empty string default
- PDF generator displays source_document in DIRECT citation formatting
- context.md JSON example includes source_document field
- All existing tests pass
- New tests verify source_document field validation and PDF rendering
- UAT Test 3 passes: DIRECT citations show italicized quotes and source document attribution
</success_criteria>

<output>
After completion, create `.planning/phases/03-citation-features-workflow/03-03-SUMMARY.md`
</output>
